import{_ as e,o as d,c as a,a2 as s}from"./chunks/framework.BwxVc06z.js";const p=JSON.parse('{"title":"ProtocolDiscrete","description":"","frontmatter":{},"headers":[],"relativePath":"tests/protocols_discrete.md","filePath":"tests/protocols_discrete.md"}'),o={name:"tests/protocols_discrete.md"};function r(c,t,i,n,l,h){return d(),a("div",null,[...t[0]||(t[0]=[s('<h1 id="protocoldiscrete" tabindex="-1">ProtocolDiscrete <a class="header-anchor" href="#protocoldiscrete" aria-label="Permalink to &quot;ProtocolDiscrete&quot;">​</a></h1><p>Phase 5c API: phase-shift keying and the correlation a finite alphabet cedes</p><p><strong>13/13 passed</strong> in 20442ms</p><table tabindex="0"><thead><tr><th>Test</th><th>What it does</th><th>Result</th></tr></thead><tbody><tr><td><code>test_bits_is_required</code></td><td><code>dm_holevo</code> requires the alphabet entropy, <code>holevo()</code> passes log2(m), and the capped value is at most the unbounded one.</td><td>✅ pass</td></tr><tr><td><code>test_both_informations</code></td><td>Both informations are reported and beta*gauss - chi_BE rebuilds the published rate, the gap under 1% at the operating point and over 10% at a bright eight-state ring.</td><td>✅ pass</td></tr><tr><td><code>test_certified_reach</code></td><td>The numerical proof certifies key where the closed form is negative, and the report carries both numbers side by side.</td><td>✅ pass</td></tr><tr><td><code>test_constellation</code></td><td><code>constellation()</code> is <code>states</code> amplitudes of modulus alpha equally spaced from the real axis, as <code>_core.dm_states</code> returns them.</td><td>✅ pass</td></tr><tr><td><code>test_discrete_costs</code></td><td>z_star sits below the Gaussian value, the rate below the Gaussian-modulation rate at the same variance, and the gap closes as states are added.</td><td>✅ pass</td></tr><tr><td><code>test_effective_plane</code></td><td>z_star, i_ab and chi_be rebuild bit for bit from the reported T_eff and xi_eff alone.</td><td>✅ pass</td></tr><tr><td><code>test_engine_agreement</code></td><td>The rate is exactly <code>_core.dm_rate</code> and the reported mutual information <code>dm_info</code>&#39;s at the effective channel, pinning the untrusted substitution T -&gt; eta<em>T, xi -&gt; xi + 2</em>v_el/(eta*T).</td><td>✅ pass</td></tr><tr><td><code>test_guards</code></td><td>The closed form refuses a trusted receiver, a homodyne and a finite block, and a two-state constellation is refused at construction.</td><td>✅ pass</td></tr><tr><td><code>test_information_wrapper</code></td><td><code>information(T, xi)</code> returns (exact, Gaussian) = (0.1107575, 0.1107648), bit for bit <code>_core.dm_info(4, 0.4, 0.5, 0.01)</code>, transposed slots give 0.0023007, and domain errors reach the caller; tree values, not a published anchor.</td><td>✅ pass</td></tr><tr><td><code>test_steps_apart</code></td><td><code>bound</code> is step 2 and is the proof, <code>upper</code> step 1&#39;s Frank-Wolfe value labelled a diagnostic, and the certificate refuses a key_rate; before a run those rows and the solver read unrun.</td><td>✅ pass</td></tr><tr><td><code>test_trusted_plane</code></td><td>A trusted receiver reaches <code>_core.dm_trusted</code> at the channel plane with efficiency and electronic noise held apart, never folded into the transmittance.</td><td>✅ pass</td></tr><tr><td><code>test_trusted_reach</code></td><td>The untrusted branch refers v_el back as xi + 2<em>v_el/(eta</em>T) and certifies nothing at T = 0.5, 0.2 or 0.05, where the trusted one certifies key at all three.</td><td>✅ pass</td></tr><tr><td><code>test_trusted_rows</code></td><td>The trusted report drops T_eff, xi_eff, key_analytic and key_gauss and returns i_ab and chi_be as None, keeping the claimed plane, the trusted flag and the certificate&#39;s rows.</td><td>✅ pass</td></tr></tbody></table>',4)])])}const u=e(o,[["render",r]]);export{p as __pageData,u as default};
