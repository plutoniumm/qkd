import{_ as e,o as d,c as a,a2 as o}from"./chunks/framework.BwxVc06z.js";const i=JSON.parse('{"title":"RrdpsLeakage","description":"","frontmatter":{},"headers":[],"relativePath":"tests/rrdps_leakage.md","filePath":"tests/rrdps_leakage.md"}'),r={name:"tests/rrdps_leakage.md"};function s(l,t,c,_,h,n){return d(),a("div",null,[...t[0]||(t[0]=[o('<h1 id="rrdpsleakage" tabindex="-1">RrdpsLeakage <a class="header-anchor" href="#rrdpsleakage" aria-label="Permalink to &quot;RrdpsLeakage&quot;">​</a></h1><p>Privacy amplification bounded by the packet length alone, no observable read</p><p><strong>9/9 passed</strong> in 179ms</p><table tabindex="0"><thead><tr><th>Test</th><th>What it does</th><th>Result</th></tr></thead><tbody><tr><td><code>test_collective_bound_beats_the_original</code></td><td><code>rrdps_collective(l, 1)</code> is exactly 1 at l = 2 and below both <code>rrdps_leak(l, 1)</code> and a whole bit at every longer train, Yin et al.&#39;s corollary.</td><td>✅ pass</td></tr><tr><td><code>test_gllp_charges_each_photon_number</code></td><td><code>rrdps_gllp</code> matches Eq. (13) summed out to four thousand photon numbers, including the thresholds past half the delays where it collapses onto the whole-key form.</td><td>✅ pass</td></tr><tr><td><code>test_leak_falls_with_train_length</code></td><td><code>rrdps_leak(l, 1)</code> falls monotonically in l, is h2(1/1023) at l = 1024, and scales as log2(l)/l with no floor.</td><td>✅ pass</td></tr><tr><td><code>test_leak_has_no_channel_argument</code></td><td>Moving the bit error rate changes <code>rrdps_rate</code> only through q<em>f_ec</em>h2(e)/l, never through privacy amplification.</td><td>✅ pass</td></tr><tr><td><code>test_leak_saturates_at_a_whole_bit</code></td><td><code>rrdps_leak</code> is exactly one bit once the threshold reaches half the delays, and stays there past it.</td><td>✅ pass</td></tr><tr><td><code>test_phase_mixes_the_error_rates</code></td><td><code>rrdps_phase</code> is the tagged share plus nu/(l - 1) on the rest, capped at one half.</td><td>✅ pass</td></tr><tr><td><code>test_source_tail_is_the_poisson_one</code></td><td><code>rrdps_src</code> is the Poisson tail of the packet&#39;s own mean l*mu, not of mu.</td><td>✅ pass</td></tr><tr><td><code>test_tagging_reduces_to_the_leak</code></td><td><code>rrdps_tag</code> passes the leakage bound through unchanged at a zero over-threshold probability, and charges a whole bit on a wholly tagged key.</td><td>✅ pass</td></tr><tr><td><code>test_three_published_forms_are_ordered</code></td><td><code>rrdps_gllp</code> &lt;= <code>rrdps_tag</code> &lt;= h2(<code>rrdps_phase</code>) at every train length and threshold.</td><td>✅ pass</td></tr></tbody></table>',4)])])}const g=e(r,[["render",s]]);export{i as __pageData,g as default};
